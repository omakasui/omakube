#!/bin/bash

# Update all installed Omabuntu themes by pulling the latest changes from their git repositories.

omakub-refresh-theme

for dir in ~/.config/omakub/themes/*/; do
  if [[ -d $dir ]] && [[ ! -L "${dir%/}" ]] && [[ -d "$dir/.git" ]]; then
    echo "Updating: $(basename "$dir")"
    git -C "$dir" pull
  fi
done
