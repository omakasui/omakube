#!/bin/bash

# Launch a floating terminal with the Omabuntu logo presentation, then execute the command passed in, and finally end with the omakub-show-done presentation.
# Used by actions such as Update System.

cmd="$*"
xdg-terminal-exec bash -c "omakub-show-logo; $cmd; if [ \$? -ne 130 ]; then omakub-show-done; fi"
